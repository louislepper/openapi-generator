package org.openapitools.model

/**
 * Sealed interface for all possible responses from getOrder
 */
sealed interface GetOrderResponse

/**
 * Sealed interface for all possible responses from updateOrder
 */
sealed interface UpdateOrderResponse

/**
 * Sealed interface for all possible responses from createUser
 */
sealed interface CreateUserResponse

/**
 * Sealed interface for all possible responses from processPayment
 */
sealed interface ProcessPaymentResponse

/**
 * Sealed interface for all possible responses from getItem
 */
sealed interface GetItemResponse

/**
 * Sealed interface for all possible responses from search
 */
sealed interface SearchResponse

